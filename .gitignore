# WezTerm portable binary and supporting files
bin/*
!bin/.gitkeep

# Taskbar shortcut (generated by install.ps1 per-machine)
Launch Claude Workspace.lnk

# DPAPI-encrypted Anthropic API key (per-user secret; never commit)
config/api-key.dat
